import moment from 'moment';

export class DateUtils {
    public static maxDatesDiffUnitAndValue(date1: Date, date2: Date) {
        const d1 = moment(date1);
        const d2 = moment(date2);
        // d1.diff(d2).
    }
}
